import { Component } from '@angular/core';

/** Stub — replaced in Chunk 9 with the full per-contest analytics view. */
@Component({
  selector: 'app-instructor-contest-detail',
  standalone: true,
  template: `
    <section style="padding:24px">
      <h1 style="font-family:var(--ff-display);color:var(--navy);margin:0 0 8px">Contest Analytics</h1>
      <p style="color:var(--muted);font-size:14px">Per-contest analytics view — coming in Chunk 9.</p>
    </section>
  `,
})
export class InstructorContestDetailComponent {}
