import { Component } from '@angular/core';

/** Stub — replaced in Chunk 8 with the full create-contest form. */
@Component({
  selector: 'app-instructor-contest-create',
  standalone: true,
  template: `
    <section style="padding:24px">
      <h1 style="font-family:var(--ff-display);color:var(--navy);margin:0 0 8px">New Contest</h1>
      <p style="color:var(--muted);font-size:14px">Create-contest form — coming in Chunk 8.</p>
    </section>
  `,
})
export class InstructorContestCreateComponent {}
